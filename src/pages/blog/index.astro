---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';

export const prerender = true;

const posts = (await getCollection('blog', ({ data }) => !data.draft)).sort(
	(a, b) => b.data.date.valueOf() - a.data.date.valueOf()
);
---

<Layout title="Blog">
	<h1 class="text-3xl font-bold tracking-tight text-slate-900 mb-2">Blog</h1>
	<p class="text-slate-600 mb-10">Thoughts, notes, and write-ups.</p>
	{posts.length === 0 && <p class="text-slate-600">No posts yet — check back soon.</p>}
	<ul class="space-y-4">
		{posts.map((post) => (
			<li>
				<a
					href={`/blog/${post.id}`}
					class="group block rounded-xl border border-slate-200 p-6 transition-all hover:border-indigo-300 hover:shadow-lg hover:shadow-indigo-100"
				>
					<p class="text-sm text-slate-400 mb-1">
						{post.data.date.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}
					</p>
					<h2 class="text-lg font-semibold text-slate-900 group-hover:text-indigo-600 transition-colors">{post.data.title}</h2>
					<p class="text-slate-600 mt-2 text-sm leading-relaxed">{post.data.description}</p>
				</a>
			</li>
		))}
	</ul>
</Layout>
